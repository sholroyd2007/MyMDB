@model ProductionLink

<div class="form-group">
    <label asp-for="MovieId">Movie</label>
    <select class="form-control" asp-for="MovieId" id="Movie">
        <option value=""></option>
        @foreach (var item in await MovieService.GetAllMovies())
        {
            <option value="@item.Id">@item.Name</option>
        }
    </select>
</div>

<div class="form-group">
    <label asp-for="TVShowId">TV Show</label>
    <select class="form-control" asp-for="TVShowId" id="TVShow">
        <option value=""></option>
        @foreach (var item in await TVService.GetAllTVShows())
        {
            <option value="@item.Id">@item.Name</option>
        }
    </select>
</div>

<div class="form-group">
    <label asp-for="ProductionCompanyId">Production Company</label>
    <select class="form-control" asp-for="ProductionCompanyId" id="ProductionCompany">
        <option value=""></option>
        @foreach (var item in await ProductionService.GetAllProductionCompanies())
        {
            <option value="@item.Id">@item.Name</option>
        }
    </select>
</div>

<div class="form-group">
    <label asp-for="TVNetworkId">TV Network</label>
    <select class="form-control" asp-for="TVNetworkId" id="TVNetwork">
        <option value=""></option>
        @foreach (var item in await ProductionService.GetAllTVNetworks())
        {
            <option value="@item.Id">@item.Name</option>
        }
    </select>
</div>


